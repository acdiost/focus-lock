{
  "name": "focus-lock",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev --features tray,custom-protocol",
    "build": "tauri build --features tray,custom-protocol",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0"
  }
}
